/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.bopro.process;

import com.bopro.database.BackendDBProcess;
import com.bo.parameter.FieldParameterMatapos;
import java.util.HashMap;
import org.apache.log4j.Logger;

/**
 *
 * @author suhanda
 */
public class additem {

    private static Logger log = Logger.getLogger(additem.class);

//    public HashMap process(HashMap input) {
//        BackendDBProcess dp = new BackendDBProcess();
//        HashMap status = dp.additem(input.get(FieldParameterMatapos.productname).toString(), input.get(FieldParameterMatapos.brand_id).toString(), input.get(FieldParameterMatapos.img_url).toString(), input.get(FieldParameterMatapos.category).toString(), input.get(FieldParameterMatapos.detailproduct).toString());
//        return status;
//    }
}
